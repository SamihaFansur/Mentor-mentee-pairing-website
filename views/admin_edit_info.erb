<%= @header %>
    <div class="background" id="statement">
      <div class="full">
        <div class="title">Edit your information</div>
          <!-- Checks if the admin information exists -->
        <% if @admins.nil? %>
            <p class="error"><strong>Unknown admin</strong></p>
        <% else %>
          <!--Checks for errors and displays any error messages -->
        <% unless @admins.errors.size.zero? %>
            <p><strong class="error">Please correct the errors below:</strong></p>
        <% end %>
        <% if @error1 %>
          <p class="error"><strong>Email exists</strong></p>
        <% end %>
          <!--Displays the edit information form -->
        <form method="post">
            <input type="hidden" name="id" value="<%= @admins.id %>" />
            <%= erb :"common/admin_edit_form" %>
        </form>
        <div class="warning">
            <!--Warning message as a deletion removes the record from the database -->
          <p><strong>WARNING! Deleting your profile cannot be undone.</strong></p>
          <form method="post" action="/deleteAdmin">
              <input type="hidden" name="id" value="<%= @admins.id %>" />
              <input type="submit" value="Delete" onclick="return confirm('Are you sure?')" />
          </form>
        </div>
<% end %>
<!--Link to the admin dashboard -->
<p class="navigation">Return to <a href="/AdminDashboard">Admin Dashboard</a>.</p>
      </div>
</div>
<%= erb :"common/footer" %>